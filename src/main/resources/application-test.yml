spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:mysql://localhost:3306/running_crew?useUnicode=true&characterEncoding=utf8
    username: running_crew
    password: running1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

domain:
  name: https://runningcrew-test.ddns.net

logging:
  file:
    name: /back_logs/back.log