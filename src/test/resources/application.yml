spring:
  datasource:
#    url: jdbc:h2:tcp://localhost/~/running-crew
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
    url: jdbc:mysql://localhost:3306/running_crew?useUnicode=true&characterEncoding=utf8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true


cloud:
  aws:
    credentials:
      accessKey:
      secretKey:
    s3:
      bucket: running-crew-test-s3
    region:
      static: ap-northeast-2
    stack:
      auto: false



logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace


s3:
  host:
    name: https://running-crew-test-s3.s3.ap-northeast-2.amazonaws.com/

fcm:
  key:
    path: fcm/crew-running-firebase-adminsdk.json

area:
  file:
    path: area/seoul.csv

jwt:
  secret-key:

domain:
  name: http://localhost:8080